name: send
on:
  schedule:
    - cron: '*/4 * * * *'
  workflow_dispatch:
jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Venus LIVE
        run: curl -s "https://venus-ai-v8.vercel.app/api/real?force=1"
