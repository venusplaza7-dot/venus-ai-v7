name: 15-per-hour
on:
  schedule:
    - cron: '*/4 * * * *'
  workflow_dispatch:
jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -s "https://venus-ai-v8.vercel.app/api/cron/auto?key=venus13" 
          curl -s "https://venus-ai-v8.vercel.app/api/real?force=true"
